const TOKEN_TYPES = {
  ACCESS_TOKEN: 'ACCESS_TOKEN',
  REFRESH_TOKEN: 'REFRESH_TOKEN',
};

const TOKEN_SECRET_MAPPER = {
  [TOKEN_TYPES.ACCESS_TOKEN]: 'ACCESS_TOKEN_SECRET',
  [TOKEN_TYPES.REFRESH_TOKEN]: 'REFRESH_TOKEN_SECRET',
};

const TOKEN_LIFE_TIME_MAPPER = {
  [TOKEN_TYPES.ACCESS_TOKEN]: 'ACCESS_TOKEN_LIFE',
  [TOKEN_TYPES.REFRESH_TOKEN]: 'REFRESH_TOKEN_LIFE',
};

const SALT_ROUNDS = 10;

const JWT_ALG = 'HS256';

const EXCEPTION_ROUTES = ['/auth/login', '/auth/refresh'];

const PERMISSION_EXCEPTION_ROUTES = [...EXCEPTION_ROUTES, '/auth/logout'];

const PERMISSION_METHOD = ['POST', 'PUT', 'DELETE'];

module.exports = {
  TOKEN_TYPES,
  TOKEN_SECRET_MAPPER,
  TOKEN_LIFE_TIME_MAPPER,
  SALT_ROUNDS,
  JWT_ALG,
  EXCEPTION_ROUTES,
  PERMISSION_EXCEPTION_ROUTES,
  PERMISSION_METHOD,
};
